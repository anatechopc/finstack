/// Settings repository
///
/// This is the repository where the settings of each user of the app is stored.
/// The entity in this repository will keep growing as we add new settings.
library;

export 'src/model/settings.dart';
export 'src/repository/settings_repository.dart';
