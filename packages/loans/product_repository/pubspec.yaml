name: product_repository
description: Product repository
version: 0.1.0+1
publish_to: none

environment:
  sdk: "^3.3.0"
  flutter: "^3.19.0"

dependencies:
  cloud_firestore: ^5.4.1
  firebase_core: ^3.4.1
  flutter:
    sdk: flutter
  json_annotation: ^4.8.1
  loooans_helpers:
    path: "../../core/loooans_helpers"
  user_loan_view_repository:
    path: "../user_loan_view_repository"

dev_dependencies:
  build_runner: ^2.4.4
  flutter_test:
    sdk: flutter
  json_serializable: ^6.7.0
  mocktail: ^1.0.3
  very_good_analysis: ^6.0.0

