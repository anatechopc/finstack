name: loooans_helpers
description: A set of helper classes, enums or functions that&#x27;s used widely in the app
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  cloud_firestore: ^5.4.1
  equatable: ^2.0.5
  firebase_database: ^11.1.4
  flutter:
    sdk: flutter
  json_annotation: ^4.8.1
  logging: ^1.2.0

dev_dependencies:
  build_runner: ^2.4.4
  fake_cloud_firestore: ^3.0.3
  flutter_test:
    sdk: flutter
  json_serializable: ^6.7.0
  mocktail: ^1.0.3
  very_good_analysis: ^6.0.0