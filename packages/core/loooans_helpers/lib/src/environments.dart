/// list of environments which firestore is running.
enum Environments {
  /// development environment
  development,
  /// staging environment
  staging,
  /// production environment
  production;
}
